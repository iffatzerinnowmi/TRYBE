*.log
.DS_Store
.env
# The pattern below was `.env.backup` — an EXACT match, so a file named
# .env.backup-before-login-fix was not ignored and got committed with a live
# API key in it. GitHub's push protection caught it. The wildcard is the fix.
.env.backup*
.env.production
.env.*.local
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
laravel
colored_changes.diff
*_routes.txt


docs/*
!docs/trybe-api-reference.md


postman/*.json


.env.backup*
